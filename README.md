# 한-글

한국어로 쓰고, 성장하고, 나만의 책을 만드는 **모바일 웹** 글쓰기 서비스입니다.

## 실행

```bash
npm install
npm run dev
```

## 검증

```bash
npm run lint
npm run build
```

## 핵심 기준

- 플랫폼: 모바일 웹 우선, `max-w-lg` 단일 앱 셸
- 화면 배경: 한지 질감(`hanji-bg`)만 사용
- 마스코트: 한이
- 주요 CTA: 태극 파랑 `#003478`
- 문서 기준: `docs/` 참조
